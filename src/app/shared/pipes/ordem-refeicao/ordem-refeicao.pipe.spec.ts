import { OrdemRefeicaoPipe } from './ordem-refeicao.pipe';

describe('OrdemRefeicaoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdemRefeicaoPipe();
    expect(pipe).toBeTruthy();
  });
});
